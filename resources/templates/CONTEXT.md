# CONTEXT.md — Project Glossary Template

Use this template as a starting point when the Analyzer discovers a new project that lacks a `CONTEXT.md` file. Keep entries concise and implementation-agnostic — the file is a glossary and decision log, not a spec.

---
## Project

- Name: <project-name>
- Path: <project-path>
- Language: php
- Framework: <cakephp|none>

## Purpose

One short sentence describing the project's business purpose and scope.

## Canonical Terms

- **Cluster**: A graph-theoretical grouping of strongly related code elements produced by the clustering algorithm.
- **Module Candidate**: A Cluster interpreted as a potentially useful architectural unit produced by Analyzer + heuristics.
- **Module**: A deliberately defined architectural boundary created by developers.
- **Bounded Context Candidate**: A hypothesis that one or more Modules represent a DDD bounded context, suggested by LLM + analysis.
- **Bounded Context**: A confirmed domain boundary with an explicit ubiquitous language and model.

## Key Domain Concepts (examples)

- Order
- OrderItem
- Customer
- Product
- Invoice

## Decisions and Policies (project-specific overrides)

- Framework nodes (e.g. `Cake\\*`, `Psr\\*`) are: [exclude|down-weight|include]  # choose one
- Dynamic resolver heuristic: [resolve|mark-dynamic|ignore]
- Clustering granularity: [class|aggregate|feature/module]
- Metric priorities: [external_coupling, outgoing_dependencies, class_count]

## Ownership Notes / Scenarios

Record important ownership decisions that affect architecture (e.g., whether `Customer` is owned inside `Order` context or an external system). Keep examples short and refer to ADRs if a decision is hard-to-reverse.

## ADRs (link or create when needed)

- ADR-0001: <short title> — <link or file path>

## Privacy / LLM Policy

- LLM usage: [local-only|blocked|allowed-with-approval]
- Note: The Analyzer produces `architecture.json` as the canonical IR. Only share reduced IR with any LLM.

## Maintainers / Contacts

- Team: <name/email>

---
Generated by Architecture Discovery Analyzer on <date>. Replace placeholders, keep the file minimal, and add ADRs for irreversible choices.
