## Agent skills

### Issue tracker

Local markdown issue files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical triage labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo layout: root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
